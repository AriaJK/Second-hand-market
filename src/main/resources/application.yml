# ?????
server:
  port: 8080
  servlet:
    context-path: /api

# Spring??
spring:
  application:
    name: secondhand-market
  datasource:
    url: jdbc:mysql://localhost:3306/market?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: gg20050105
    driver-class-name: com.mysql.cj.jdbc.Driver

# MyBatis??
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.compus.secondhandmarket.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# ????
logging:
  level:
    com.compus.secondhandmarket.mapper: debug